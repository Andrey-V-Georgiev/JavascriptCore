{{> header}}
<div class="content">
    {{> navbar}}
    {{> notifications}}
    <section id="viewCatalog">
        <div class="posts">
            {{#each posts}}
                {{> post}}
            {{/each}}
        </div>
    </section>
</div>
{{> footer}}