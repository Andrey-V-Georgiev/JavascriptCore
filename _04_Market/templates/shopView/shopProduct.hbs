<tr>
    <th>{{name}}</th>
    <th>{{description}}</th>
    <th>{{price}}</th>
    <th>
        <form action="#/purchase/:{{_id}}" method="post">
            <button type="submit">Purchase</button>
        </form>
    </th>
</tr>